# Factory Dashboard

Optional Render static dashboard (alternative to the SuperPlane Console).
Reads run telemetry from factory-postgres / ingest-gateway and shows live KPIs:
auto-built PoCs, EASY vs COMPLEX ratio, build funnel, avg repair attempts.
